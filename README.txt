1. Run 01_INSTALL.cmd once.
2. Run 03_RUN_APP.cmd to open the interface.
3. Run 02_TRAIN_MODELS.cmd only when the five localization models must be trained again.

The training script runs exactly 100 epochs for each of the five models.
The OCR component is installed with the Python packages; no separate OCR program is required.
